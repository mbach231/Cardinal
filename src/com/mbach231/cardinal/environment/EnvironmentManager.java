
package com.mbach231.cardinal.environment;

import com.mbach231.cardinal.CardinalLogger;
import com.mbach231.cardinal.environment.season.Season;
import com.mbach231.cardinal.environment.season.climate.ClimateListener;
import com.mbach231.cardinal.environment.season.growth.GrowthListener;
import java.util.HashSet;
import java.util.Set;
import org.bukkit.event.Listener;

/**
 *
 *
 */
public class EnvironmentManager {

/*
    private final GrowthListener growthListener_;
    private final ClimateListener climateListener_;

    public EnvironmentManager() {
        
        CardinalLogger.log(CardinalLogger.LogID.Initialization, "Initializing plant growth...");
        growthListener_ = new GrowthListener();
        
        CardinalLogger.log(CardinalLogger.LogID.Initialization, "Initializing climate...");
        climateListener_ = new ClimateListener();
    }
    
    // Given a season, load other env components
    public void loadSeason(Season season) {
        
    }
    
    public Set<Listener> getListeners() {
        Set<Listener> listeners = new HashSet();
        
        listeners.add(growthListener_);
        listeners.add(climateListener_);
        
        return listeners;
        
    }*/
}
