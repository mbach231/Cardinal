
package com.mbach231.cardinal.environment;

import org.bukkit.event.Listener;

/**
 *
 * 
 */
public class EnvironmentListener implements Listener {
/*
    private final EnvironmentManager environmentManager_;
    private final DateManager dateManager_;
    private final SeasonManager seasonManager_;
    
    public EnvironmentListener() {
        environmentManager_ = new EnvironmentManager();
        
        seasonManager_ = new SeasonManager();
        dateManager_ = new DateManager(seasonManager_.getSeasons());
    }
    
    public Set<Listener> getListeners() {
        Set<Listener> listeners = environmentManager_.getListeners();
        listeners.add(this);
        return listeners;
    }
    */
}
